name: Dictadito build host smoke

on:
  push:
    branches:
      - dictadito-build-host
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "public runner works"
